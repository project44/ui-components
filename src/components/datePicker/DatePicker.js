import React from 'react';
import { DatePicker as AntDatePicker } from 'antd';

const DatePicker = (props) => {
  const { } = props;
    return (
      <AntDatePicker placeholder="Select Date" className="custom-button-link">

      </AntDatePicker>
    );
};

export default DatePicker;
